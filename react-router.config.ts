import type { Config } from "@react-router/dev/config";

export default {
  // SSR mode is REQUIRED for this template.
  // It enables server-side API routes that keep API keys safe.
  // Do NOT change this to false unless you understand the security implications.
  ssr: true,
} satisfies Config;
